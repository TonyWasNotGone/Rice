# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hyprlock config for < 1080p monitor resolutions                                                         
# Original config submitted by https://github.com/SherLock707

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf
$Scripts = $HOME/.config/hypr/scripts

general {
    grace = 1
    fractional_scaling = 2
    immediate_render = true
}

background {
    monitor =
    # NOTE: use only 1 path
	#path = screenshot   # screenshot of your desktop
	#path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_modified # by wallpaper effects
    path = $HOME/.config/hypr/wallpaper_effects/SHITASSV1BUTARMS.png # current wallpaper
    
    color = rgb(0,0,0) # color will be rendered initially until path is available

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    #blur_size = 3
    #blur_passes = 2 # 0 disables blurring
    #noise = 0.0117
    #contrast = 1.3000 # Vibrant!!!
    #brightness = 0.8000
    #vibrancy = 0.2100
    #vibrancy_darkness = 0.0
}

# Battery
label {
    monitor =
    text = cmd[update:] acpi | cut -d, -f2 | tr -d ' '
    color = rgb(255,255,255)
    color = rgb(255,255,255)
    font_size = 40
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = -516, -288
    #halign = center
    #valign = top
}

# Date
label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%A, %-d %B')" </b>"
    color =rgb(255,255,255)
    font_size = 40
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = -516, -173
    #halign = center
    #valign = center
}


# Minute-Time
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H")":"$(date +"%M")":"$(date +"%S")"
    color = rgb(255,255,255)
    color = rgb(255,255,255)
    font_size = 40
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = -516, -63
    #halign = center
    #valign = top
}


# Put a picture of choice here. Default is the current wallpaper
#image {
#    monitor =
#    path = $HOME/.config/hypr/wallpaper_effects/.wallpaper_current
#    size = 160
#    rounding = -1
#    border_size = 0
#    border_color = $color11
#    rotate = 0
#    reload_time = -1
#    position = 0, 280
#    halign = center
#    valign = bottom
}

# USER
label {
    monitor =
    text =  $USER
    color = rgb(128, 128, 128)
    font_size = 24
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 125, 260
    halign = left
    #valign = bottom
}

# INPUT FIELD
input-field {
    monitor =
    size = 600, 130
    outline_thickness = 2
    dots_size = 0.2 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.2 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color = rgba(255, 255, 255, 0)
	capslock_color = rgba(255,255,255, 0)
    font_color =rgb(0, 0, 0)
    fade_on_empty = false
    font_family = JetBrainsMono Nerd Font ExtraBold
    placeholder_text = <i><span foreground="##000000">Enter Boot Sequence...</span></i>
    hide_input = false
    position = -516, 53
    #halign = center
    #valign = bottom
}

# Keyboard LAYOUT
label {
    monitor =
    text = $LAYOUT
    color =rgb(128, 128, 128)
    font_size = 24
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 131, -365
    halign = left
    #valign = bottom
}

# uptime
label {
    monitor =
    text = cmd[update:60000] echo "<b> "$(uptime -p || $Scripts/UptimeNixOS.sh)" </b>"
    color = rgb(179, 179, 179)
    font_size = 30
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = 109, -410
    halign = left
    #valign = bottom
}

# weather edit the scripts for locations
# weather scripts are located in ~/.config/hypr/UserScripts Weather.sh and/or Weather.py
# see https://github.com/JaKooLit/Hyprland-Dots/wiki/TIPS#%EF%B8%8F-weather-app-related-for-waybar-and-hyprlock
#label {
    monitor =
    text = cmd[update:3600000] [ -f "$HOME/.cache/.weather_cache" ] && cat "$HOME/.cache/.weather_cache"
    color = rgb(255,255,255)
    font_size = 40
    font_family = JetBrainsMono Nerd Font ExtraBold
    position = -516, -283
    #halign = left
    #valign = bottom
}
